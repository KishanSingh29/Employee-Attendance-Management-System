spring.application.name=attendanceservice
server.port=8082

# ===== Datasource =====
spring.datasource.url=jdbc:mysql://localhost:3306/attendanceservice_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===== JPA / Hibernate =====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ===== JWT (validation only - tokens are issued by authservice) =====
jwt.secret=mySecretKey123456789012345678901234567890

# ===== Attendance business rules =====
attendance.late-threshold=09:30:00
attendance.half-day-hours=4

# ===== Kafka (consumer) =====
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=attendance-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.value.default.type=com.attendance.attendanceservice.event.UserRegisteredEvent
spring.kafka.consumer.properties.spring.json.trusted.packages=*
spring.kafka.consumer.properties.spring.json.use.type.headers=false
spring.kafka.topic.user-registered=user-registered

# ===== Swagger / OpenAPI =====
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# ===== Error handling =====
server.error.include-message=always
server.error.include-binding-errors=always
